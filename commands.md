# Useful Commands

- pwd: print the path we are currently in
- ls: lists differents files and folder
- git init: initialize Git
- git status: print state of files (modified, staged etc)
- git add: add files in the staging zone
- git commit: record changes with a message
 there are also other commands that exists to navigate in the powershell like mkdir, it will create a folder